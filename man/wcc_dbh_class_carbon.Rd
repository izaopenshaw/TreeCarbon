% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/C_WCC.R
\name{wcc_dbh_class_carbon}
\alias{wcc_dbh_class_carbon}
\title{Calculate carbon from DBH class distribution (WCC Method E)}
\usage{
wcc_dbh_class_carbon(
  name,
  tally_data,
  type = NULL,
  area_ha = NULL,
  method = "Matthews2",
  biome = "temperate",
  output.all = TRUE,
  re_dbh = NULL,
  re_h = NULL,
  re = 0.025,
  nsg = NULL,
  sig_nsg = 0.09413391,
  height_timber = NULL,
  timber_ratio = 0.85,
  rich_output = FALSE
)
}
\arguments{
\item{name}{Species name(s), either binomial or common. Can be:
\itemize{
\item Single value (applied to all classes)
\item Vector (one per class, same length as tally data)
}}

\item{tally_data}{Data frame from \code{wcc_process_tally()} containing DBH
class information, or a data frame with columns: dbh_midpoint, count, height}

\item{type}{Either 'broadleaf' or 'conifer' (single value or vector, same length as tally_data)}

\item{area_ha}{Area of the stand/plot in hectares. If NULL, assumes data
represents trees per hectare (i.e., counts are already per hectare).}

\item{method}{Method of converting biomass to carbon. Default \code{"Matthews2"}
(recommended for WCC). Also: \code{"Thomas"}, \code{"IPCC2"}.}

\item{biome}{Temperate, boreal, mediterranean, tropical, subtropical}

\item{output.all}{If TRUE, outputs carbon per class and total. If FALSE, outputs total only.}

\item{re_dbh}{Relative measurement error for diameter at breast height (single value).}

\item{re_h}{Relative error of height measurement (single value).}

\item{re}{Relative error of coefficients (default = 0.025).}

\item{nsg}{Nominal specific gravity. Optionally specified, else will use that
given by the WCC.}

\item{sig_nsg}{Sigma for nominal specific gravity (NSG) or wood density.}

\item{height_timber}{Optional: Timber height(s) for broadleaf species. Can be
a single value or vector matching tally_data rows.}

\item{timber_ratio}{Ratio for estimating timber height from total height when
height_timber is not provided (default = 0.85).}

\item{rich_output}{Logical. If TRUE, returns a rich result object with
metadata. Default FALSE.}
}
\value{
If \code{rich_output = FALSE}: either total stand AGC in tonnes,
or if output.all = TRUE, a data.frame with carbon per class and total.
If \code{rich_output = TRUE}: an \code{allometry_result} object with metadata.
}
\description{
Calculates total stand carbon from DBH class tallies using WCC
Method E (Full Tree Count). This function takes processed DBH class tallies
and calculates carbon for each class, then sums to get total stand carbon.
}
\details{
This function implements WCC Method E (Full Tree Count) for stands where
trees are tallied by DBH class rather than measured individually. It:
\enumerate{
\item Calculates carbon for each DBH class using representative DBH and height
\item Multiplies by the number of trees in each class
\item Sums across all classes to get total stand carbon
}

\strong{Input Requirements:}
\itemize{
\item DBH class midpoints (representative DBH for each class)
\item Counts of trees in each class
\item Heights (either provided or will be estimated)
\item Species information
}

\strong{Height Handling:}
\itemize{
\item If heights are missing, they should be estimated using height-DBH
relationships or measured for representative trees
\item For broadleaves, timber height is required (or will be estimated)
\item For conifers, total height is used
}

\strong{Scaling:}
\itemize{
\item If area_ha is provided, counts are assumed to be for that area
\item If area_ha is NULL, counts are assumed to already represent trees per hectare
}
}
\examples{
# Example 1: Process tally and calculate carbon
dbh_midpoints <- c(5, 15, 25, 35, 45)
counts <- c(150, 200, 180, 120, 50)
heights <- c(3, 8, 15, 20, 22)

# Process tally
tally_data <- wcc_process_tally(
  dbh_class = dbh_midpoints,
  count = counts,
  height = heights
)

# Calculate carbon
carbon_result <- wcc_dbh_class_carbon(
  name = "Oak",
  tally_data = tally_data,
  type = "broadleaf",
  area_ha = 0.1,
  output.all = TRUE
)
print(carbon_result)

# Example 2: With uncertainty propagation
carbon_with_error <- wcc_dbh_class_carbon(
  name = "Sitka Spruce",
  tally_data = tally_data,
  type = "conifer",
  area_ha = 0.1,
  re_dbh = 0.05,
  re_h = 0.1,
  output.all = TRUE
)
print(carbon_with_error)
}
\references{
Jenkins, Thomas AR, et al. "FC Woodland Carbon Code:
Carbon Assessment Protocol (v2. 0)." (2018). Method E.
}
\author{
Justin Moat. J.Moat@kew.org, Isabel Openshaw. I.Openshaw@kew.org
}
