% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/C_WCC.R
\name{wcc_stratify}
\alias{wcc_stratify}
\title{Combine carbon estimates from stratified sampling (WCC)}
\usage{
wcc_stratify(
  strata_data,
  area_col = "area_ha",
  carbon_col = NULL,
  sigma_col = NULL
)
}
\arguments{
\item{strata_data}{Data.frame with columns area_ha and either total_carbon_t
or carbon_per_ha_t. Or a list of such data.frames. For uncertainty: include
total_sig_carbon_t (when using total_carbon_t) or carbon_per_ha_sig_t
(when using carbon_per_ha_t).}

\item{area_col}{Character. Name of area column (default "area_ha")}

\item{carbon_col}{Character. Name of carbon column - "total_carbon_t" or
"carbon_per_ha_t" (default auto-detected)}

\item{sigma_col}{Character. Name of sigma column - "total_sig_carbon_t" or
"carbon_per_ha_sig_t". Default auto-detected from strata_data.}
}
\value{
List with total_area_ha, total_carbon_t, weighted_mean_carbon_per_ha_t.
When sigma provided: total_sig_carbon_t, weighted_mean_sig.
}
\description{
Aggregate carbon across strata using area-weighted averaging.
For projects with multiple stands or strata. Propagates uncertainty when
sigma columns are provided.
}
\details{
Uncertainty propagation (assuming independent strata):
\itemize{
\item When using total_carbon_t: Var(total) = sum(sigma_total_i^2)
\item When using carbon_per_ha_t: Var(total) = sum(area_i^2 * sigma_per_ha_i^2)
\item sigma_weighted_mean = total_sig_carbon_t / total_area_ha
}
}
\seealso{
\code{\link{wcc_error_propagation}} for error propagation rules.
}
