% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FC_Carbon.R
\name{fc_agc_error}
\alias{fc_agc_error}
\title{Calculate above ground carbon with error propagation (WCC)}
\usage{
fc_agc_error(
  name,
  dbh,
  height,
  type = NULL,
  method = "IPCC2",
  biome = "temperate",
  output.all = TRUE,
  re_dbh = 0.05,
  re_h = 0.1,
  re = 0.025,
  nsg = NULL,
  sig_nsg = 0.09413391,
  rich_output = FALSE
)
}
\arguments{
\item{name}{species name, either binomial or common}

\item{dbh}{diameter at breast height in centimetres}

\item{height}{tree height in metres}

\item{type}{either 'broadleaf' or 'conifer'}

\item{method}{method of converting biomass to carbon. Either 'Thomas' or 'IPCC2' as these specify the error associated with the carbon volatile fraction}

\item{biome}{temperate, boreal, mediterranean, tropical, subtropical or all}

\item{output.all}{if TRUE outputs all data from processing, else just outputs carbon estimates}

\item{re_dbh}{relative measurement error for diameter at breast height, single value}

\item{re_h}{relative error of height measurement, single value}

\item{re}{relative error of coefficients (default = 2.5\%)}

\item{nsg}{nominal specific gravity. Optionally specified, else will use that
given by the WCC}

\item{sig_nsg}{sigma for nominal specific gravity (NSG) or wood density}

\item{rich_output}{Logical. If TRUE, returns a rich result object with full
metadata including: value, method, citation, assumptions, validity_warning,
flags, uncertainty interval, region, and source type. Default FALSE.}
}
\value{
If \code{rich_output = FALSE}: either Above ground carbon (AGC) in tonnes,
or if output.all = TRUE, a data.frame with tariff number, volumes, biomass,
carbon estimates and associated uncertainties (sig_*).
If \code{rich_output = TRUE}: an \code{allometry_result} object with full metadata.
}
\description{
Function that inputs tree species code, dbh, height and method
for converting biomass to carbon, and returns the carbon estimate with
propagated uncertainty using Woodland Carbon Code methodology.

When \code{rich_output = TRUE}, returns a comprehensive result object
including method metadata, assumptions, validity warnings, and uncertainty
}
\examples{
fc_agc_error(name='Quercus robur', dbh=74, height=24, output.all = FALSE)
fc_agc_error('Oak', dbh=74, height=24, method="IPCC2",
biome="temperate", output.all = FALSE, re_dbh=1, re_h=1)
# Input wood density and sd from BIOMASS package
wd <- BIOMASS::getWoodDensity('Quercus', 'robur', region='Europe')
fc_agc_error('Oak', 72, 24, nsg = wd$meanWD, sig_nsg = wd$sdWD)

# Rich output with full metadata
result <- fc_agc_error('Oak', 50, 20, rich_output = TRUE)
print(result)
}
\references{
Jenkins, Thomas AR, et al. "FC Woodland Carbon Code:
Carbon Assessment Protocol (v2. 0)." (2018).
}
\author{
Justin Moat. J.Moat@kew.org, Isabel Openshaw I.Openshaw@kew.org
}
