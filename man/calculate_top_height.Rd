% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/WCC_functions.R
\name{calculate_top_height}
\alias{calculate_top_height}
\title{Calculate stand top height from tree data}
\usage{
calculate_top_height(dbh, height, area_ha = NULL, re_h = NULL, n_trees = 100)
}
\arguments{
\item{dbh}{Diameter at breast height in centimetres (vector of tree measurements)}

\item{height}{Tree height in metres (vector of tree measurements, same length as dbh)}

\item{area_ha}{Area of the woodland/stand in hectares. If NULL, assumes data
represents trees per hectare (i.e., length(dbh) = trees per hectare)}

\item{re_h}{Relative error of height measurement (optional, for uncertainty calculation)}

\item{n_trees}{Number of largest trees per hectare to use (default = 100, as per WCC protocol)}
}
\value{
If re_h is NULL: numeric value of top height in metres. If re_h is
provided: data.frame with top_height, sigma (uncertainty), and re_h
(relative error for use in stand_tariff).
}
\description{
Calculates top height (mean height of the 100 largest trees per
hectare by DBH) from woodland tree data, as defined in WCC Protocol Method D
(Section 4.1.4). Top height is a specific forestry measurement required for
stand-level carbon assessments when individual tree DBH measurements are not
available.
}
\details{
Top height is defined in WCC Protocol v2.0 (Section 4.1.4) as the mean height
of the 100 largest trees per hectare (by DBH). This is a stand-level measurement
used in Method D when individual tree DBH measurements are not available for
all trees.

The function calculates top height by:
\enumerate{
\item Determining trees per hectare (from area_ha or assuming data is per hectare)
\item Selecting the n_trees largest trees by DBH (or all trees if fewer than n_trees)
\item Calculating the mean height of those trees
\item Returning the top height with optional uncertainty
}

\strong{When to use this function:}
\itemize{
\item You have tree DBH and height measurements from a sample plot
\item You want to calculate top height from plot data for Method D
\item You're doing stand-level carbon assessment from plot samples
}

\strong{When NOT to use this function:}
\itemize{
\item You have measured top height directly in the field → Use \code{stand_tariff()} directly
\item You have DBH and height for all trees → Use \code{fc_agc()} for individual trees (Method B/C/E)
\item You want individual tree carbon estimates → Use \code{fc_agc()}
}

\strong{Note:} Top height can be measured directly in the field (standard forestry practice)
without needing individual tree measurements. This function is a helper for when
you have plot data and want to calculate top height from it.
}
\examples{
# Example 1: Stand with known area
dbh <- c(45, 52, 38, 60, 42, 55, 48, 35, 58, 40)  # 10 trees
height <- c(18, 22, 15, 24, 17, 21, 19, 14, 23, 16)
area_ha <- 0.1  # 0.1 hectare plot

top_h <- calculate_top_height(dbh, height, area_ha)
print(top_h)

# Example 2: With uncertainty
top_h_unc <- calculate_top_height(dbh, height, area_ha, re_h = 0.05)
print(top_h_unc)

# Example 3: Data already represents trees per hectare (omit area_ha)
dbh_ha <- c(rep(45, 50), rep(52, 30), rep(38, 20))  # 100 trees/ha
height_ha <- c(rep(18, 50), rep(22, 30), rep(15, 20))
top_h2 <- calculate_top_height(dbh_ha, height_ha, area_ha = NULL)

# Example 4: Use top height with stand_tariff
spcode <- "OK"  # Oak
tariff <- stand_tariff(spcode, stand_height = top_h)
}
\references{
Jenkins, Thomas AR, et al. "FC Woodland Carbon Code:
Carbon Assessment Protocol (v2. 0)." (2018). Section 4.1.4, Method D.
}
\author{
Justin Moat. J.Moat@kew.org, Isabel Openshaw. I.Openshaw@kew.org
}
